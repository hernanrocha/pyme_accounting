<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_import_purchase_excel" model="ir.actions.server">
        <field name="name">Importar Compras desde Excel</field>
        <field name="model_id" ref="model_l10n_ar_import_purchase_excel"></field>
        <field name="state">code</field>
        <!-- Se debe definir un action para que se ejecute -->
        <field name="code">action = model.action_import_menu()</field>
    </record>

</odoo>